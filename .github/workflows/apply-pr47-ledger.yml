name: Apply PR 47 ledger update

on:
  pull_request:

permissions:
  contents: write

jobs:
  apply:
    if: github.event.pull_request.head.ref == 'agent/automatic-long-media-transcription' && github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Check out feature branch
        uses: actions/checkout@v4
        with:
          ref: agent/automatic-long-media-transcription
          fetch-depth: 0
      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - name: Install dependencies
        run: bun install --frozen-lockfile
      - name: Update canonical ledgers
        run: node scripts/apply-pr47-ledger.mjs
      - name: Format ledgers
        run: bunx prettier --write TASKS.md .github/workflows/ci.yml
      - name: Remove one-shot ledger files
        run: rm .github/workflows/apply-pr47-ledger.yml scripts/apply-pr47-ledger.mjs
      - name: Commit ledger update
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "docs: register reviewed automatic transcription"
          git push origin HEAD:agent/automatic-long-media-transcription
