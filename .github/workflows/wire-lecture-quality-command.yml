name: Retired lecture quality command wiring

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary wiring workflow retired."
